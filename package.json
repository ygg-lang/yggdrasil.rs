{
    "private": true,
    "scripts": {
        "t":  "wee generate -t && wee test",
        "test": "cd projects/tree-sitter-yg && tree-sitter test",
        "generate": "cd projects/tree-sitter-yg && tree-sitter generate"
    }
}
