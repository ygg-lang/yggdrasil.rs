{
    "private": true,
    "scripts": {
        "t":  "wee generate -t && wee test",
        "test": "cd projects/tree-sitter-ygg && tree-sitter test",
        "generate": "cd projects/tree-sitter-ygg && tree-sitter generate",
        "lsp": "cd projects/ygg-lsp && cargo install --path . --debug",
        "cli": "cd projects/ygg-cli && cargo install --path . --debug"
    }
}
