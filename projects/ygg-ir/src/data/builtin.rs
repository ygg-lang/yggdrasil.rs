use super::*;

impl ExpressionKind {
    pub fn rule(name: &str) -> Self {
        let data = match name {
            "XID_START" | "XID_CONTINUE" => {
                todo!()
            }
            _ => {
                todo!();
            }
        };
    }
}
