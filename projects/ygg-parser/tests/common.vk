namespace test;
let a = 1⁏
let mut ma = 1⁏

function test(
    // 1
    #[nice(#boat args: what)]
    // 2
    placeholder mut a: A = 0,
    // 3
    `b`: `B` = null
) -> ReturnType {
    if err != nil {
        return a
    }
}
