use super::*;

pub fn code_lens_provider(args: CodeLensParams) -> Option<Vec<CodeLens>> {
    //let cfg = ParserConfig::default();
    // let ast = match cfg.parse(&read_url(&args.text_document.uri)) {
    //     Ok(o) => o,
    //     Err(_) => return None,
    // };
    // Some(ast.toc(9).build_code_lens())
    return None
}
